# 宿主节点健康机制关键点勘误与修正

> 本文档针对《节点健康状态与指标流动机制分析》中三个关键点的勘误，基于源码逐条验证后给出修正结论。

---

## 一、maintenance_mode 的实际影响范围

### 1.1 原分析中的错误

原文称：
> 节点进入维护模式后，用户访问服务器会显示维护页面；但不影响面板 API 与守护进程的通信。

这一描述在多处不准确，需要逐场景修正。

### 1.2 源码验证：maintenance_mode 在各场景中的真实行为

#### 场景 A：客户端 API 访问（/api/client/servers/{server}/*）

**实际生效机制**：不是通过 `MaintenanceMiddleware` 中间件，而是通过 `AuthenticateServerAccess` 中间件。

**源码证据**：`app/Http/Middleware/Api/Client/Server/AuthenticateServerAccess.php:49-61`

```php
try {
    $server->validateCurrentState();
} catch (ServerStateConflictException $exception) {
    // 例外 1：查看服务器详情始终放行
    if (!$request->routeIs('api:client:server.view')) {
        // 例外 2：挂起或维护模式下，查看资源使用也放行
        if (($server->isSuspended() || $server->node->isUnderMaintenance())
            && !$request->routeIs('api:client:server.resources')) {
            throw $exception;
        }
        // 例外 3：root_admin 访问 websocket 也放行
        if (!$user->root_admin || !$request->routeIs($this->except)) {
            throw $exception;
        }
    }
}
```

**修正后的影响范围**：

| 路由 | 维护模式下行为 | 依据 |
|------|---------------|------|
| `GET /api/client/servers/{server}` (view) | **放行** — 用户始终可查看服务器详情 | 第 54 行：`routeIs('api:client:server.view')` 例外 |
| `GET /api/client/servers/{server}/resources` | **放行** — 用户可查看资源使用 | 第 55 行：`routeIs('api:client:server.resources')` 例外 |
| `GET /api/client/servers/{server}/websocket` | **root_admin 放行**，其他用户拦截 | 第 58 行：`routeIs($this->except)` 即 `api:client:server.ws` |
| 其他所有客户端 API | **拦截** — 抛出 `ServerStateConflictException` | 默认逻辑 |

**ServerStateConflictException 中的维护模式消息**：`app/Exceptions/Http/Server/ServerStateConflictException.php:19-20`

```php
} elseif ($server->node->isUnderMaintenance()) {
    $message = 'The node of this server is currently under maintenance and the functionality requested is unavailable.';
}
```

**关键发现**：`Server::validateCurrentState()` 会因维护模式抛出异常，但 `AuthenticateServerAccess` 中间件对 `view` 和 `resources` 路由做了显式例外。这意味着**维护模式下的用户仍然可以看到服务器信息和资源使用量，但不能执行任何写操作**。

#### 场景 B：MaintenanceMiddleware 的实际使用状态

**源码证据**：`app/Http/Kernel.php:105`

```php
'node.maintenance' => MaintenanceMiddleware::class,
```

**关键发现**：`MaintenanceMiddleware` 仅被注册为中间件别名 `node.maintenance`，但在整个路由定义中（`routes/api-client.php`、`routes/api-remote.php`、`routes/admin.php` 等）**没有任何路由或中间件组实际使用它**。

搜索所有路由文件和控制器：
```
grep -rn "node.maintenance" routes/ → 无结果
grep -rn "MaintenanceMiddleware" app/Http/ → 仅 Kernel.php 注册
```

**结论**：`MaintenanceMiddleware` 是一个**已注册但未使用的中间件**。维护模式的实际拦截逻辑完全由 `AuthenticateServerAccess::handle()` 中的 `validateCurrentState()` + 路由名例外实现，**而非**独立的中间件拦截。原文称"显示维护页面"的说法基于 `MaintenanceMiddleware` 返回 `errors.maintenance` 视图，但该中间件实际上并未被应用到任何路由上。

#### 场景 C：自动部署筛选

**源码证据**：`app/Services/Deployment/FindViableNodesService.php:74-78`

```php
$query = Node::query()->select('nodes.*')
    ->selectRaw('IFNULL(SUM(servers.memory), 0) as sum_memory')
    ->selectRaw('IFNULL(SUM(servers.disk), 0) as sum_disk')
    ->leftJoin('servers', 'servers.node_id', '=', 'nodes.id')
    ->where('nodes.public', 1);
```

**关键发现**：`FindViableNodesService` 的筛选条件**仅检查 `public = 1`**，**完全不检查 `maintenance_mode`**。这意味着**处于维护模式的节点仍然会出现在自动部署的候选列表中**，服务器可能被自动分配到维护中的节点。

**这是一个潜在的设计缺陷**：`public = 1` 控制是否允许自动部署，`maintenance_mode` 控制是否允许用户操作，两者语义不同但自动部署时没有联动。

#### 场景 D：定时任务执行

**源码证据**：`app/Jobs/Schedule/RunTaskJob.php:48-57`

```php
$server = $this->task->server;
if (!is_null($server->status)) {
    $this->failed();
    return;
}
```

**关键发现**：`RunTaskJob` 仅检查 `server->status` 是否为 `null`，**不检查节点 `maintenance_mode`**。维护模式不会阻止定时任务的执行，定时任务会继续尝试向守护进程发送命令。

但存在间接影响：如果节点处于维护模式，守护进程通常仍在运行，定时任务的命令仍可成功执行。维护模式只是阻止了用户通过客户端 API 操作，并不停止守护进程。

#### 场景 E：守护进程远程 API

**源码证据**：`routes/api-remote.php`

远程 API 的中间件组为 `daemon`（仅包含 `SubstituteBindings` + `DaemonAuthenticate`），**不包含维护模式检查**。守护进程的所有回调接口（安装状态、传输状态、SFTP 认证等）在维护模式下**完全正常工作**。

### 1.3 修正总结

| 场景 | 原分析 | 修正后 |
|------|--------|--------|
| 客户端 API | "显示维护页面" | 返回 JSON `ServerStateConflictException`，但 view 和 resources 路由例外放行 |
| 实际拦截机制 | `MaintenanceMiddleware` | `AuthenticateServerAccess` 中间件内联逻辑 |
| 自动部署 | 未提及 | **不影响**，维护中节点仍可被自动选中 |
| 定时任务 | 未提及 | **不影响**，定时任务不检查维护模式 |
| 远程 API | "不影响" | 确认不影响，但原因更明确：daemon 中间件组无维护检查 |
| Middleware 使用状态 | 作为拦截中间件使用 | **已注册但未使用**，是遗留代码 |

---

## 二、retry_after 与作业重试策略的语义关联

### 2.1 原分析中的混淆

原文将 `retry_after` 描述为"任务可重试窗口"，并构建了如下重试时间表：

> | 尝试次数 | 延迟 |
> |----------|------|
> | 第 1 次 | 立即 |
> | 第 2 次 | 10 秒后 |
> | 第 3 次 | 30 秒后 |

这个时间表存在**语义混淆**：`retry_after` 和 `$this->release()` 是两套独立的机制，原文将它们混为一谈。

### 2.2 retry_after 的真实语义

**源码**：`config/queue.php:68`

```php
'redis' => [
    'retry_after' => (int) env('REDIS_QUEUE_RETRY_AFTER', 90),
],
```

**Laravel 框架语义**：`retry_after` 是队列驱动的**超时保护机制**，而非重试间隔。它的含义是：

> 如果一个作业被取出后超过 `retry_after` 秒仍未被确认（ack），队列驱动会认为该 worker 已崩溃，将该作业重新放回队列供其他 worker 消费。

**关键区别**：
- `retry_after = 90` 表示：作业被取出 90 秒后若未完成/确认，**自动重新入队**
- 这与 `$tries` 控制的**显式重试次数**是完全独立的机制
- `retry_after` 的触发场景是 **worker 崩溃/卡死**，而非正常的 `release()` 或 `failed()`

### 2.3 release() 与 retry_after 的交互

**RevokeSftpAccessJob 中的 release()**：`app/Jobs/RevokeSftpAccessJob.php:50-54`

```php
} catch (DaemonConnectionException) {
    $this->release($this->attempts() * 10);
}
```

**`$this->release($delay)` 的语义**：将当前作业**重新放回队列**，并指定延迟 `$delay` 秒后再被消费。这是**显式的、应用层的重试**。

**交互关系**：

1. 作业执行 → `DaemonConnectionException` → `release(10)` → 作业延迟 10 秒后重新可消费
2. 此时作业的 `attempts()` 递增
3. `retry_after = 90` 与此**无关**——它只在 worker 取出作业后**静默崩溃**（无 ack、无 release、无 failed）时才介入
4. 只有在作业卡在 worker 中超过 90 秒时，`retry_after` 才会将其重新入队，导致**非预期的重复执行**

### 2.4 正确的重试时间线

```
第 1 次执行
    ├─→ DaemonConnectionException
    ├─→ release(1 * 10 = 10)  →  延迟 10 秒后重新入队
    │
第 2 次执行 (attempts = 2)
    ├─→ DaemonConnectionException
    ├─→ release(2 * 10 = 20)  →  延迟 20 秒后重新入队
    │
第 3 次执行 (attempts = 3)
    ├─→ DaemonConnectionException
    ├─→ release(3 * 10 = 30)  →  延迟 30 秒后重新入队
    │   但此时 attempts(3) >= tries(3)
    │   Laravel 在 release 后检查 tries 限制
    │   → 标记为 failed，不再重试
```

**重要纠正**：原文表格中"第 3 次：30 秒后"的描述暗示第 3 次会在 30 秒后执行，但实际上第 3 次执行失败后 `release(30)` 虽然被调用，但 Laravel 会检查 `attempts() >= tries` 并直接标记为失败。第 3 次尝试是**立即执行**的（从队列取出），30 秒延迟的 release 不会生效因为 tries 限制优先。

### 2.5 RunTaskJob 的重试策略差异

**关键发现**：`RunTaskJob` **没有定义 `$tries` 属性**，也没有 `release()` 调用。它的异常处理逻辑是：

```php
catch (\Exception $exception) {
    if (!($this->task->continue_on_failure && $exception instanceof DaemonConnectionException)) {
        throw $exception;  // 直接抛出 → Laravel 标记为 failed
    }
    // continue_on_failure = true 时静默继续
}
```

**这意味着**：
- `RunTaskJob` **不会自动重试**（Laravel 默认 `tries = 1`）
- 即使 `continue_on_failure = true`，也只是跳过当前任务继续执行下一个任务，而非重试当前任务
- `retry_after = 90` 对 `RunTaskJob` 仅在 worker 崩溃时起保护作用

### 2.6 两套机制对比

| 维度 | `retry_after` | `release()` + `tries` |
|------|---------------|----------------------|
| 触发条件 | Worker 静默崩溃/超时 | 应用代码显式调用 |
| 控制方 | 队列驱动（框架层） | 作业代码（应用层） |
| 语义 | 防止作业丢失 | 实现业务重试策略 |
| 交互 | 独立于 attempts 计数 | 递增 attempts，受 tries 限制 |
| 适用场景 | 基础设施故障保护 | 守护进程临时不可达时的优雅重试 |

---

## 三、/api/servers/reset 调用条件的源码验证与推断识别

### 3.1 源码可验证的事实

#### 可验证点 1：路由注册

**源码**：`routes/api-remote.php:10`

```php
Route::post('/servers/reset', [Remote\Servers\ServerDetailsController::class, 'resetState']);
```

**验证结论**：该端点是一个 `POST` 请求，属于 `daemon` 中间件组，需要 Wings 提供 Bearer Token 认证。

#### 可验证点 2：认证要求

**源码**：`app/Http/Middleware/Api/Daemon/DaemonAuthenticate.php:34-66`

所有 `daemon` 中间件组的路由（除 `daemon.configuration` 外）都要求：
- 请求头包含 `Authorization: Bearer {token_id}.{token}`
- `token_id` 对应有效的 `Node.daemon_token_id`
- `token` 与 `Node.daemon_token` 解密后匹配

**验证结论**：`/servers/reset` 的调用方必须是已注册的节点，且必须携带正确的认证凭据。

#### 可验证点 3：端点功能逻辑

**源码**：`app/Http/Controllers/Api/Remote/Servers/ServerDetailsController.php:89-134`

功能逻辑可逐行验证：
1. 从请求属性获取认证节点 → `$node = $request->attributes->get('node')`
2. 查找该节点上 `status = restoring_backup` 的服务器
3. 对有备份恢复活动记录的服务器，记录 `server:backup.restore-failed` 活动日志
4. 批量更新该节点上 `status IN (installing, restoring_backup)` 的服务器为 `status = null`
5. 返回 `204 No Content`

#### 可验证点 4：操作范围限定

**源码**：`Server::query()->where('node_id', $node->id)`

**验证结论**：该端点仅影响发起请求的节点上的服务器，不会影响其他节点。

#### 可验证点 5：PHPDoc 中的调用说明

**源码**：`ServerDetailsController.php:82-85` 注释

```php
/**
 * Resets the state of all servers on the node to be normal. This is triggered
 * when Wings restarts and is useful for ensuring that any servers on the node
 * do not get incorrectly stuck in installing/restoring from backup states since
 * a Wings reboot would completely stop those processes.
 */
```

**验证结论**：面板源码中的注释说明此端点"Wings 重启时触发"，但这是**面板侧的文档注释**，不是 Wings 的调用代码。

### 3.2 基于推断的结论（无法从本仓库源码验证）

#### 推断点 1：Wings 在启动时调用此端点

**推断依据**：
- PHPDoc 注释明确写道 "This is triggered when Wings restarts"
- 路由设计放在 `api-remote.php`（专门给 Wings 调用的路由文件）
- 端点语义是重置因 Wings 重启而中断的安装/恢复状态

**无法验证原因**：Wings 的 Go 源码不在本仓库中。实际的调用时机、频率和条件需要查看 Wings 源码才能确认。

**推断可信度**：**高** — 注释、路由归属和端点语义三者一致。

#### 推断点 2：Wings 不会在其他时机调用此端点

**无法验证原因**：Wings 可能在其他场景（如配置变更、手动触发等）也调用此端点。面板侧无法得知调用方的上下文。

**推断可信度**：**中** — 端点名为 `resetState`，语义偏向启动时清理，但不排除其他调用场景。

#### 推断点 3：Wings 启动后、调用 /servers 之前调用此端点

**推断依据**：逻辑上，Wings 启动后应先重置面板上的异常状态，再拉取服务器列表，以避免拉到不一致的状态。

**无法验证原因**：调用顺序由 Wings 决定，面板侧无感知。

**推断可信度**：**中** — 合理的推测但缺乏源码证实。

#### 推断点 4：Wings 不会在运行中反复调用此端点

**推断依据**：如果 Wings 运行中反复调用，会不断将正常安装中的服务器状态重置，导致正在进行的安装被误判为完成。

**无法验证原因**：Wings 是否有防重入保护或调用频率控制不在本仓库范围内。

**推断可信度**：**高** — 反复调用会导致数据不一致，设计上应避免。

### 3.3 源码可验证与推断对照表

| 结论 | 类型 | 依据 | 可信度 |
|------|------|------|--------|
| 端点需要节点 Bearer Token 认证 | ✅ 可验证 | `DaemonAuthenticate` 中间件 | 确定 |
| 仅影响请求节点上的服务器 | ✅ 可验证 | `where('node_id', $node->id)` | 确定 |
| 重置 `installing` 和 `restoring_backup` 状态 | ✅ 可验证 | `whereIn('status', [...])` | 确定 |
| 对恢复中的备份记录失败活动日志 | ✅ 可验证 | `Activity::event('server:backup.restore-failed')` | 确定 |
| Wings 在启动时调用此端点 | ⚠️ 推断 | PHPDoc 注释 + 路由归属 | 高 |
| Wings 不在运行中反复调用 | ⚠️ 推断 | 调用会导致数据不一致 | 高 |
| Wings 在拉取服务器列表前调用 | ⚠️ 推断 | 逻辑顺序推测 | 中 |
| Wings 不在其他场景调用 | ⚠️ 推断 | 无正面证据 | 中 |

### 3.4 对原文的修正

原文表述：
> 触发时机：Wings 守护进程启动时自动调用

**修正为**：
> 面板源码的 PHPDoc 注释说明此端点"Wings 重启时触发"，但实际调用时机依赖 Wings 端的实现，无法从面板源码独立验证。面板侧仅保证：认证合法的节点调用时，正确重置该节点上的异常服务器状态。

---

## 四、修正后的关键源码索引补充

| 模块 | 文件路径 | 修正说明 |
|------|----------|----------|
| 客户端访问控制 | `app/Http/Middleware/Api/Client/Server/AuthenticateServerAccess.php` | 维护模式的**实际生效点**，非 `MaintenanceMiddleware` |
| 状态冲突异常 | `app/Exceptions/Http/Server/ServerStateConflictException.php` | 维护模式下的具体错误消息定义 |
| 中间件注册 | `app/Http/Kernel.php:105` | `node.maintenance` 别名注册但**未使用** |
| 客户端路由 | `routes/api-client.php:57-64` | 服务器相关路由的中间件组不含 `node.maintenance` |
| 部署服务 | `app/Services/Deployment/FindViableNodesService.php:78` | 仅过滤 `public`，**不过滤** `maintenance_mode` |
| 定时任务 | `app/Jobs/Schedule/RunTaskJob.php:53` | 仅检查 `server->status`，**不检查** `maintenance_mode` |
| 队列配置 | `config/queue.php:68` | `retry_after` 是超时保护，非重试间隔 |
| SFTP 撤销任务 | `app/Jobs/RevokeSftpAccessJob.php:54` | `release()` 是显式重试，与 `retry_after` 独立 |
| 重置端点 | `app/Http/Controllers/Api/Remote/Servers/ServerDetailsController.php:82-85` | PHPDoc 说明调用时机，但面板侧无法验证实际调用 |
