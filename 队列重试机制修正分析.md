# 队列重试机制修正分析

> 本文档针对《节点健康机制关键点勘误与修正》中关于队列重试的结论进行修正，基于仓库中的 queue worker 启动参数和作业类实现，重新判断两个 Job 的最大尝试次数、release/backoff 的生效范围，以及 attempts 达到上限时的最终操作。

---

## 一、队列 Worker 启动参数

### 1.1 Supervisor 配置（Docker 部署场景）

**源码位置**：`.github/docker/supervisord.conf:27-28`

```ini
[program:queue-worker]
command=/usr/local/bin/php /app/artisan queue:work --queue=high,standard,low --sleep=3 --tries=3
user=nginx
autostart=true
autorestart=true
```

**启动参数解析**：

| 参数 | 值 | 含义 |
|------|-----|------|
| `--queue` | `high,standard,low` | 队列优先级：high > standard > low |
| `--sleep` | `3` | 无作业时等待 3 秒再轮询 |
| `--tries` | `3` | **全局默认最大尝试次数** |

### 1.2 关键区别：`--tries=3` 与作业类 `$tries` 的优先级

Laravel 的优先级规则：
1. 作业类上定义的 `$tries` 属性 **优先于** `queue:work --tries` 命令行参数
2. 若作业类未定义 `$tries`，则使用 `queue:work --tries` 的值作为默认
3. 若两者都未定义，Laravel 默认不限制尝试次数（无限重试）

---

## 二、RunTaskJob 的重试行为分析

### 2.1 作业类定义（源码层面）

**源码位置**：`app/Jobs/Schedule/RunTaskJob.php`

```php
class RunTaskJob implements ShouldQueue
{
    use Queueable;
    use DispatchesModels;
    use SerializesModels;

    // 未定义 $tries
    // 未定义 $backoff
    // 未定义 $retryUntil
    // 未定义 $maxExceptions

    public function __construct(public Task $task, public bool $manualRun = false)
    {
        $this->queue = 'standard';
    }

    public function failed(?\Exception $exception = null)
    {
        $this->markTaskNotQueued();
        $this->markScheduleComplete();
    }
}
```

**源码可确认的事实**：
- **无 `$tries` 属性**：不自行限制尝试次数
- **无 `release()` 调用**：不会显式重新入队
- **无 `$backoff` 属性**：无重试间隔定义
- **有 `failed()` 方法**：Laravel 在尝试次数耗尽后调用此方法

### 2.2 实际最大尝试次数

| 部署场景 | `--tries` | 作业 `$tries` | 实际 tries | 依据 |
|----------|-----------|---------------|------------|------|
| Docker 部署 | 3 | 未定义 | **3** | 命令行参数生效 |
| 手动启动 `queue:work` 无 `--tries` | 未指定 | 未定义 | **无限** | Laravel 默认行为 |
| 手动启动 `queue:work --tries=1` | 1 | 未定义 | **1** | 命令行参数生效 |

**结论**：RunTaskJob 的最大尝试次数**不是源码层面的确定值**，而是**运行时配置决定的**。Docker 部署下为 3 次，但其他部署场景可能不同。

### 2.3 RunTaskJob 的异常处理路径

```
RunTaskJob::handle()
    │
    ├─→ schedule 不活跃 && 非手动 → 正常结束（不计为失败）
    │
    ├─→ server->status != null → 调用 $this->failed() → 正常结束
    │   注意：此处 $this->failed() 是手动调用，不是 Laravel 的重试失败回调
    │   手动调用 failed() 不会触发 Laravel 的重试计数逻辑
    │
    ├─→ 执行任务成功 → markTaskNotQueued() + queueNextTask()
    │
    └─→ 执行任务异常
         ├─→ continue_on_failure && DaemonConnectionException
         │   → 静默继续，markTaskNotQueued() + queueNextTask()
         │   → **不抛异常，不触发重试**
         │
         └─→ 其他异常
              → throw $exception
              → Laravel 捕获，递增 attempts
              → 若 attempts < tries → 重新入队（无延迟，无 backoff）
              → 若 attempts >= tries → 调用 failed() → 标记为失败作业
```

### 2.4 RunTaskJob 重试时的行为特征

**源码确认**：
- **无 backoff**：RunTaskJob 未定义 `$backoff`，重试时**立即**重新入队，无延迟
- **无 `release()` 调用**：重试由 Laravel 框架驱动，非应用层控制
- **`failed()` 回调**：当 attempts 达到上限时，Laravel 调用 `failed()` 方法，执行 `markTaskNotQueued()` 和 `markScheduleComplete()`
- **`continue_on_failure` 的影响**：仅对 `DaemonConnectionException` 生效，且效果是**跳过而非重试**

### 2.5 关键修正：`$this->failed()` 的手动调用

**源码位置**：`app/Jobs/Schedule/RunTaskJob.php:53-54`

```php
if (!is_null($server->status)) {
    $this->failed();
    return;
}
```

**修正**：这里的 `$this->failed()` 是**应用代码手动调用**的方法，不是 Laravel 的失败回调。区别在于：

| 维度 | Laravel 调用 `failed()` | 应用代码手动调用 `failed()` |
|------|------------------------|---------------------------|
| 触发 | attempts >= tries 后由框架调用 | 代码中显式调用 |
| 递增 attempts | 是（框架已完成递增） | 否（与框架重试计数无关） |
| 作业状态 | 标记为 failed | 作业**正常结束**（`return` 后框架认为成功） |
| 是否写入 failed_jobs 表 | 是 | **否** |

这意味着当 `server->status != null` 时，RunTaskJob 不会被 Laravel 视为失败，也不会重试，而是一次性成功结束（只是内部执行了清理逻辑）。

---

## 三、RevokeSftpAccessJob 的重试行为分析

### 3.1 作业类定义（源码层面）

**源码位置**：`app/Jobs/RevokeSftpAccessJob.php`

```php
class RevokeSftpAccessJob implements ShouldQueue, ShouldBeUnique
{
    use Queueable;

    public int $tries = 3;
    public int $maxExceptions = 1;

    // 未定义 $backoff
    // 未定义 $retryUntil

    public function handle(DaemonRevocationRepository $repository): void
    {
        try {
            $repository->setNode($node)->deauthorize($this->user, $targetUuids);
        } catch (DaemonConnectionException) {
            $this->release($this->attempts() * 10);
        }
    }
}
```

**源码可确认的事实**：
- **`$tries = 3`**：自行定义最大尝试次数，**覆盖** `queue:work --tries` 参数
- **`$maxExceptions = 1`**：允许的最大未捕获异常次数为 1
- **无 `$backoff` 属性**：无框架级重试间隔定义
- **有 `release()` 调用**：应用层控制的重试，带延迟

### 3.2 实际最大尝试次数

| 部署场景 | `--tries` | 作业 `$tries` | 实际 tries | 依据 |
|----------|-----------|---------------|------------|------|
| Docker 部署 | 3 | 3 | **3** | 作业类 `$tries` 优先 |
| 手动启动 `--tries=5` | 5 | 3 | **3** | 作业类 `$tries` 优先 |
| 手动启动无 `--tries` | 未指定 | 3 | **3** | 作业类 `$tries` 生效 |

**结论**：RevokeSftpAccessJob 的最大尝试次数**由源码确定**为 3 次，不受运行时 `--tries` 参数影响。

### 3.3 `release()` 与 `tries` 的交互机制

**Laravel 框架行为**（非本仓库代码，但为 Laravel 标准行为）：

1. `release($delay)` 将作业放回队列，并递增 `attempts` 计数器
2. Worker 取出作业时，在执行 `handle()` 之前检查 `attempts >= tries`
3. 若 `attempts >= tries`，**不执行 `handle()`**，直接调用 `failed()` 并标记为失败

**关键时序**：

```
第 1 次执行（attempts=1 从队列取出）
    ├─→ DaemonConnectionException
    └─→ release(1*10=10) → 延迟 10 秒后重新入队

第 2 次执行（attempts=2 从队列取出）
    ├─→ DaemonConnectionException
    └─→ release(2*10=20) → 延迟 20 秒后重新入队

第 3 次执行（attempts=3 从队列取出）
    ├─→ DaemonConnectionException
    └─→ release(3*10=30) → 作业重新入队
    但是！下次 worker 取出时 attempts=4 >= tries=3
    → Laravel 不执行 handle()，直接调用 failed()
    → 作业被标记为失败，写入 failed_jobs 表
```

**重要修正**：`release()` 调用本身**不会检查 tries 限制**，它只是将作业放回队列。tries 限制的检查发生在 **worker 取出作业时**。因此第 3 次 `release(30)` **确实会被执行**，作业确实会被延迟 30 秒后放回队列，但下次取出时因 `attempts(4) >= tries(3)` 而被框架直接标记失败。

### 3.4 `maxExceptions = 1` 的作用

`maxExceptions` 控制**未捕获异常**的最大次数。在 RevokeSftpAccessJob 中：

- `DaemonConnectionException` 被 `catch` 块捕获并调用 `release()`，**不计入 maxExceptions**
- 其他未捕获的异常（如序列化错误、类型错误等）才计入 maxExceptions
- 当未捕获异常次数达到 `maxExceptions = 1` 时，作业**立即标记为失败**

**与 `tries` 的区别**：
- `tries = 3`：限制**总尝试次数**（包括 `release()` 导致的重试）
- `maxExceptions = 1`：限制**未捕获异常次数**（排除 `release()` 导致的重试）

### 3.5 `ShouldBeUnique` 接口的影响

```php
class RevokeSftpAccessJob implements ShouldQueue, ShouldBeUnique
```

`ShouldBeUnique` 确保同一时刻队列中只有一个相同 `uniqueId` 的作业：

```php
public function uniqueId(): string
{
    return "revoke-sftp:{$this->user}:{$target}";
}
```

**影响**：
- 作业释放（`release()`）回队列后，仍持有 unique 锁
- 不会因重复调度而产生多个相同的撤销任务
- 作业成功完成或最终失败后，unique 锁释放

---

## 四、两个作业的重试行为对比

### 4.1 综合对比表

| 维度 | RunTaskJob | RevokeSftpAccessJob |
|------|------------|---------------------|
| **最大尝试次数** | 运行时配置（Docker: 3） | **源码确定**: 3 |
| **tries 来源** | `queue:work --tries=3` | 作业类 `$tries = 3` |
| **是否受 `--tries` 影响** | **是** | 否（类定义优先） |
| **应用层重试** | 无（`continue_on_failure` 是跳过） | 有（`release()` 显式重试） |
| **backoff/延迟** | 无（重试无延迟） | 有（`attempts() * 10` 秒线性退避） |
| **框架层重试** | 异常抛出后由框架驱动 | 仅在 `release()` 后框架取出时驱动 |
| **attempts 达上限操作** | 调用 `failed()` → 清理 task/schedule | 框架调用 `failed()` → 写入 failed_jobs |
| **`failed()` 方法** | 有（手动+框架调用） | 无显式定义（使用 Laravel 默认） |
| **maxExceptions** | 未定义（默认无限） | 1 |
| **Uniqueness** | 无 | `ShouldBeUnique` |
| **continue_on_failure** | 支持（仅 DaemonConnectionException） | 不适用 |

### 4.2 RunTaskJob 的完整生命周期

```
调度触发 (ProcessRunnableCommand → 每分钟)
    │
    ├─→ dispatch(RunTaskJob) → 进入 standard 队列
    │
    ├─→ Worker 取出 (attempts=1)
    │    ├─→ schedule 不活跃 → 正常结束
    │    ├─→ server->status != null → 手动 failed() → 正常结束
    │    ├─→ 任务执行成功 → queueNextTask() → 正常结束
    │    ├─→ DaemonConnectionException + continue_on_failure → 跳过 → 正常结束
    │    └─→ 其他异常 → throw → 框架捕获
    │         ├─→ attempts < tries → 立即重新入队（无延迟）
    │         └─→ attempts >= tries → 框架调用 failed() → 写入 failed_jobs
    │
    └─→ [Docker 部署: tries=3] 最多重试 2 次，共 3 次执行
```

### 4.3 RevokeSftpAccessJob 的完整生命周期

```
用户删除/密码变更 → dispatch(RevokeSftpAccessJob)
    │
    ├─→ Worker 取出 (attempts=1)
    │    ├─→ deauthorize 成功 → 正常结束
    │    └─→ DaemonConnectionException → release(10) → 延迟 10 秒
    │
    ├─→ Worker 取出 (attempts=2)
    │    ├─→ deauthorize 成功 → 正常结束
    │    └─→ DaemonConnectionException → release(20) → 延迟 20 秒
    │
    ├─→ Worker 取出 (attempts=3)
    │    ├─→ deauthorize 成功 → 正常结束
    │    └─→ DaemonConnectionException → release(30) → 延迟 30 秒
    │         （release 执行成功，作业放回队列）
    │
    └─→ Worker 取出 (attempts=4)
         → attempts(4) >= tries(3)
         → 框架直接标记为失败
         → 写入 failed_jobs 表
         → 不执行 handle()
```

**时间线**（假设每次都失败）：

```
T+0s    : 第 1 次执行 → release(10)
T+10s   : 第 2 次执行 → release(20)
T+30s   : 第 3 次执行 → release(30)
T+60s   : 第 4 次取出 → 直接失败（不执行 handle）
```

---

## 五、源码结论与运行配置结论的区分

### 5.1 仅基于源码可确认的结论

| 结论 | 依据 |
|------|------|
| RevokeSftpAccessJob 的 `$tries = 3`，不受 `--tries` 影响 | 类属性定义，Laravel 优先级规则 |
| RevokeSftpAccessJob 使用 `release()` 实现应用层重试，延迟为 `attempts * 10` 秒 | `handle()` 中 catch 块 |
| RevokeSftpAccessJob 的 `maxExceptions = 1`，1 次未捕获异常即终止 | 类属性定义 |
| RevokeSftpAccessJob 的 `release()` 会递增 attempts，但 tries 检查发生在 worker 取出时 | Laravel 框架标准行为 |
| RunTaskJob 未定义 `$tries`，其最大尝试次数由运行配置决定 | 类无 `$tries` 属性 |
| RunTaskJob 无应用层重试（`release()`），重试由框架驱动 | 类无 `release()` 调用 |
| RunTaskJob 的 `continue_on_failure` 是跳过而非重试 | 源码逻辑：跳过后继续 queueNextTask |
| RunTaskJob 的 `$this->failed()` 手动调用不触发框架重试计数 | 作业正常 return 结束 |
| RunTaskJob 重试时无延迟（未定义 `$backoff`） | 类无 `$backoff` 属性 |

### 5.2 基于运行配置的结论（依赖部署方式）

| 结论 | 依赖条件 | 不满足时 |
|------|----------|----------|
| RunTaskJob 最多尝试 3 次 | Docker 部署使用 supervisord.conf | 手动启动时取决于 `--tries` 参数 |
| RunTaskJob 的 `--sleep=3` | Docker 部署 | 手动启动默认 `--sleep=3` |
| 队列优先级为 high > standard > low | Docker 部署 | 取决于 `--queue` 参数 |
| `retry_after = 90` 秒 | Redis 队列连接 + 默认配置 | 取决于 `QUEUE_CONNECTION` 和 `REDIS_QUEUE_RETRY_AFTER` |

### 5.3 前文勘误中需要二次修正的内容

| 原勘误结论 | 需修正为 |
|------------|----------|
| "RunTaskJob 不会自动重试（Laravel 默认 tries = 1）" | RunTaskJob 未定义 `$tries`，Docker 部署下 **tries = 3**（来自 `--tries=3`），会自动重试；仅手动启动且无 `--tries` 时才默认不限制 |
| "第 3 次 release(30) 虽然被调用，但 Laravel 会检查 attempts >= tries 并直接标记为失败" | 第 3 次 `release(30)` **确实执行了**，作业被放回队列；但第 4 次取出时 `attempts(4) >= tries(3)`，框架**不执行 handle()**，直接标记失败 |
| "release() 与 retry_after 独立" | 修正：两者确实独立，但需补充说明——当 `release()` 的延迟 > `retry_after` 时，`retry_after` 会抢先重新入队，导致**非预期的额外 attempts 递增**。RevokeSftpAccessJob 的 `release(30)` 在第 3 次调用时不会触发此问题（30 < 90），但若自定义更大的 release 延迟则需注意 |

---

## 六、关键源码索引

| 模块 | 文件路径 | 关键内容 |
|------|----------|----------|
| Supervisor 配置 | `.github/docker/supervisord.conf:27-28` | `queue:work --tries=3 --sleep=3` |
| Docker 入口脚本 | `.github/docker/entrypoint.sh` | 无额外队列参数 |
| Dockerfile | `Dockerfile:37` | 复制 supervisord.conf 到容器 |
| RunTaskJob | `app/Jobs/Schedule/RunTaskJob.php` | 无 `$tries`/`$backoff`，有 `failed()` |
| RevokeSftpAccessJob | `app/Jobs/RevokeSftpAccessJob.php` | `$tries=3`，`$maxExceptions=1`，`release()` |
| 队列配置 | `config/queue.php:68` | `retry_after=90`（Redis） |
