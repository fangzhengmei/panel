# 宿主节点健康状态与上报指标流动机制分析

## 1. 系统架构概述

Pterodactyl 面板采用**面板(Panle) + 守护进程(Wings)** 的分布式架构：
- **面板端**：基于 Laravel 框架，负责管理节点、服务器、用户等核心业务逻辑
- **守护进程(Wings)**：运行在每个宿主节点上，负责实际的服务器容器管理
- **通信方式**：通过 HTTP API 进行双向通信，使用 Bearer Token 认证

## 2. 心跳与采样频率机制

### 2.1 连接与超时配置

**配置文件位置**：`config/pterodactyl.php:79-82`

```php
'guzzle' => [
    'timeout' => env('GUZZLE_TIMEOUT', 15),        // 请求超时 15 秒
    'connect_timeout' => env('GUZZLE_CONNECT_TIMEOUT', 5),  // 连接超时 5 秒
],
```

### 2.2 资源指标采样频率

**客户端资源查询接口**：`app/Http/Controllers/Api/Client/Servers/ResourceUtilizationController.php:23-40`

```php
public function __invoke(GetServerRequest $request, Server $server): array
{
    $key = "resources:$server->uuid";
    $stats = $this->cache->remember($key, Carbon::now()->addSeconds(20), function () use ($server) {
        return $this->repository->setServer($server)->getDetails();
    });
    // ...
}
```

**关键设计点**：
- 采用**拉模式**：面板向守护进程主动拉取指标
- **缓存机制**：指标缓存 **20 秒**，避免频繁请求
- 缓存键格式：`resources:{server_uuid}`

### 2.3 守护进程配置拉取

**节点配置接口**：`app/Http/Controllers/Api/Application/Nodes/NodeConfigurationController.php:17-20`

守护进程启动时通过此接口获取配置信息，包含：
- 节点 UUID 和认证 Token
- API 监听端口和 SSL 配置
- 数据目录和 SFTP 配置
- 面板回调地址

### 2.4 定时任务调度

**定时任务配置**：`app/Console/Kernel.php:29-75`

| 任务 | 频率 | 说明 |
|------|------|------|
| 调度任务执行 | 每分钟 | 处理服务器定时任务 |
| 备份文件清理 | 每日 | 清理服务备份文件 |
| 孤立备份清理 | 每30分钟 | 删除异常备份 |
| 遥测数据上报 | 每日 | 匿名使用统计 |

### 2.5 守护进程心跳：从触发到上报的完整路径

#### 2.5.1 前端心跳触发（管理后台）

**节点列表页心跳逻辑**：`resources/views/admin/nodes/index.blade.php:79-105`

```javascript
(function pingNodes() {
    $('td[data-action="ping"]').each(function(i, element) {
        $.ajax({
            type: 'GET',
            url: $(element).data('location'),  // {scheme}://{fqdn}:{daemonListen}/api/system
            headers: {
                'Authorization': 'Bearer ' + $(element).data('secret'),
            },
            timeout: 5000  // 前端 AJAX 超时 5 秒
        }).done(function (data) {
            // 成功：显示绿色心跳动画 + 版本号提示
            $(element).removeClass('text-muted').find('i')
                .removeClass().addClass('fa fa-fw fa-heartbeat faa-pulse animated')
                .css('color', '#50af51');
        }).fail(function (error) {
            // 失败：显示红色空心爱心 + 错误提示
            $(element).removeClass('text-muted').find('i')
                .removeClass().addClass('fa fa-fw fa-heart-o')
                .css('color', '#d9534f');
        });
    }).promise().done(function () {
        setTimeout(pingNodes, 10000);  // 每 10 秒轮询一次
    });
})();
```

**前端心跳频率**：10 秒 / 次
**前端超时判定**：5 秒无响应即判定为失败

#### 2.5.2 面板后端 API 调用链

**守护进程 HTTP 客户端配置**：`app/Repositories/Wings/DaemonRepository.php:49-64`

```php
public function getHttpClient(array $headers = []): Client
{
    return new Client([
        'verify' => $this->app->environment('production'),
        'base_uri' => $this->node->getConnectionAddress(),
        'timeout' => config('pterodactyl.guzzle.timeout'),              // 默认 15 秒
        'connect_timeout' => config('pterodactyl.guzzle.connect_timeout'), // 默认 5 秒
        'headers' => array_merge($headers, [
            'Authorization' => 'Bearer ' . $this->node->getDecryptedKey(),
            'Accept' => 'application/json',
            'Content-Type' => 'application/json',
        ]),
    ]);
}
```

**超时配置参数**：`config/pterodactyl.php:79-82`

| 参数 | 默认值 | 说明 | 环境变量 |
|------|--------|------|----------|
| `connect_timeout` | 5 秒 | TCP 连接超时 | `GUZZLE_CONNECT_TIMEOUT` |
| `timeout` | 15 秒 | 整个请求超时 | `GUZZLE_TIMEOUT` |

**配置校验规则**：`app/Http/Requests/Admin/Settings/AdvancedSettingsFormRequest.php:18-19`
- 两个超时值必须在 1-60 秒之间

#### 2.5.3 队列任务重试机制

**队列配置**：`config/queue.php:40-71`

```php
'redis' => [
    'driver' => 'redis',
    'retry_after' => (int) env('REDIS_QUEUE_RETRY_AFTER', 90),  // 90 秒后任务视为失败可重试
    'block_for' => null,
    'after_commit' => false,
],
```

**SFTP 权限撤销重试示例**：`app/Jobs/RevokeSftpAccessJob.php:18-56`

```php
class RevokeSftpAccessJob implements ShouldQueue, ShouldBeUnique
{
    public int $tries = 3;           // 最大重试次数
    public int $maxExceptions = 1;   // 最大异常次数

    public function handle(DaemonRevocationRepository $repository): void
    {
        try {
            $repository->setNode($node)->deauthorize($this->user, $targetUuids);
        } catch (DaemonConnectionException) {
            // 线性退避：第 1 次 10 秒，第 2 次 20 秒，第 3 次后放弃
            $this->release($this->attempts() * 10);
        }
    }
}
```

**重试策略**：
| 尝试次数 | 延迟 | 判定逻辑 |
|----------|------|----------|
| 第 1 次 | 立即 | 失败后延迟 10 秒重试 |
| 第 2 次 | 10 秒后 | 失败后延迟 20 秒重试 |
| 第 3 次 | 30 秒后 | 失败则标记任务为失败 |
| 最终 | - | 3 次失败后停止，假设节点完全离线 |

#### 2.5.4 心跳状态判定逻辑

**状态图标映射**：
| 状态 | 图标 | 颜色 | 说明 |
|------|------|------|------|
| 加载中 | `fa-refresh fa-spin` | 灰色 | 正在请求中 |
| 在线 | `fa-heartbeat faa-pulse` | 绿色 `#50af51` | 5 秒内响应成功 |
| 离线 | `fa-heart-o` | 红色 `#d9534f` | 超时或返回错误 |

**心跳上报完整链路**：
```
管理后台 (浏览器)
    │  [每 10 秒]
    ├─→ AJAX GET /api/system  (5秒超时)
    │    └─→ Wings 守护进程
    │        ├─→ 200 OK + 版本号 → 绿色心跳
    │        └─→ 超时/错误 → 红色空心心
    │
面板后端 (PHP)
    │  [按需触发]
    ├─→ Guzzle HTTP Client
    │    ├─→ connect_timeout: 5秒 (TCP 连接)
    │    └─→ timeout: 15秒 (完整请求)
    │
    └─→ 队列任务
         ├─→ retry_after: 90秒 (任务可重试窗口)
         ├─→ tries: 3次 (最大尝试次数)
         └─→ 退避策略: attempts * 10秒 (线性退避)
```

## 3. 指标在服务端的聚合方法

### 3.1 节点资源使用统计

**NodeRepository::getUsageStats()**：`app/Repositories/Eloquent/NodeRepository.php:22-49`

```php
public function getUsageStats(Node $node): array
{
    $stats = $this->getBuilder()
        ->selectRaw('IFNULL(SUM(servers.memory), 0) as sum_memory, IFNULL(SUM(servers.disk), 0) as sum_disk')
        ->join('servers', 'servers.node_id', '=', 'nodes.id')
        ->where('node_id', '=', $node->id)
        ->first();
    // ...
}
```

**聚合逻辑**：
1. 通过 `SUM()` 聚合该节点下所有服务器的内存和磁盘分配
2. 计算超配比后的最大可用资源：`max = node.memory * (1 + node.memory_overallocate / 100)`
3. 计算使用百分比：`percent = (sum / max) * 100`
4. 返回颜色标记：
   - `green`：使用量 ≤ 75%
   - `yellow`：75% < 使用量 ≤ 90%
   - `red`：使用量 > 90%

### 3.2 原始指标格式（StatsTransformer）

**指标转换器**：`app/Transformers/Api/Client/StatsTransformer.php:18-32`

```php
public function transform(array $data): array
{
    return [
        'current_state' => Arr::get($data, 'state', 'stopped'),
        'is_suspended' => Arr::get($data, 'is_suspended', false),
        'resources' => [
            'memory_bytes' => Arr::get($data, 'utilization.memory_bytes', 0),
            'cpu_absolute' => Arr::get($data, 'utilization.cpu_absolute', 0),
            'disk_bytes' => Arr::get($data, 'utilization.disk_bytes', 0),
            'network_rx_bytes' => Arr::get($data, 'utilization.network.rx_bytes', 0),
            'network_tx_bytes' => Arr::get($data, 'utilization.network.tx_bytes', 0),
            'uptime' => Arr::get($data, 'utilization.uptime', 0),
        ],
    ];
}
```

**上报指标字段说明**：
| 字段 | 类型 | 说明 |
|------|------|------|
| `memory_bytes` | int | 实际内存使用（字节） |
| `cpu_absolute` | float | CPU 使用百分比 |
| `disk_bytes` | int | 磁盘使用（字节） |
| `network_rx_bytes` | int | 网络接收（字节） |
| `network_tx_bytes` | int | 网络发送（字节） |
| `uptime` | int | 运行时长（毫秒） |

### 3.3 节点资源使用查询（调度用）

**NodeRepository::getNodeWithResourceUsage()**：`app/Repositories/Eloquent/NodeRepository.php:142-151`

```php
public function getNodeWithResourceUsage(int $node_id): Node
{
    return $this->getBuilder()
        ->select(['nodes.id', 'nodes.fqdn', 'nodes.scheme', ...])
        ->selectRaw('IFNULL(SUM(servers.memory), 0) as sum_memory, IFNULL(SUM(servers.disk), 0) as sum_disk')
        ->leftJoin('servers', 'servers.node_id', '=', 'nodes.id')
        ->where('nodes.id', $node_id)
        ->first();
}
```

## 4. 健康标记对调度和告警的影响

### 4.1 节点可行性检查

**Node::isViable()**：`app/Models/Node.php:242-249`

```php
public function isViable(int $memory, int $disk): bool
{
    $memoryLimit = $this->memory * (1 + ($this->memory_overallocate / 100));
    $diskLimit = $this->disk * (1 + ($this->disk_overallocate / 100));

    return ($this->sum_memory + $memory) <= $memoryLimit 
        && ($this->sum_disk + $disk) <= $diskLimit;
}
```

### 4.2 自动部署节点筛选

**FindViableNodesService::handle()**：`app/Services/Deployment/FindViableNodesService.php:69-99`

筛选条件：
1. `nodes.public = 1` - 只选择公开节点
2. 指定位置（可选）
3. 内存超配后仍有足够空间
4. 磁盘超配后仍有足够空间

```sql
HAVING (IFNULL(SUM(servers.memory), 0) + ?) <= (nodes.memory * (1 + (nodes.memory_overallocate / 100)))
HAVING (IFNULL(SUM(servers.disk), 0) + ?) <= (nodes.disk * (1 + (nodes.disk_overallocate / 100)))
```

### 4.3 服务器状态约束

**Server 状态常量**：`app/Models/Server.php:122-126`

```php
public const STATUS_INSTALLING = 'installing';
public const STATUS_INSTALL_FAILED = 'install_failed';
public const STATUS_REINSTALL_FAILED = 'reinstall_failed';
public const STATUS_SUSPENDED = 'suspended';
public const STATUS_RESTORING_BACKUP = 'restoring_backup';
```

**状态验证逻辑**：`app/Models/Server.php:390-401`

```php
public function validateCurrentState()
{
    if (
        $this->isSuspended()                    // 已挂起
        || $this->node->isUnderMaintenance()    // 节点维护中
        || !$this->isInstalled()                // 未安装完成
        || $this->status === self::STATUS_RESTORING_BACKUP  // 恢复备份中
        || !is_null($this->transfer)            // 迁移中
    ) {
        throw new ServerStateConflictException($this);
    }
}
```

### 4.4 维护模式影响

**MaintenanceMiddleware**：`app/Http/Middleware/MaintenanceMiddleware.php:20-31`

- 节点进入维护模式后，用户访问服务器会显示维护页面
- 但不影响面板 API 与守护进程的通信
- 通过 `node.maintenance_mode` 字段控制

### 4.5 超配（Over-Allocation）机制

**超配计算**：
- 允许超配比例：`memory_overallocate` / `disk_overallocate`（百分比）
- `-1` 表示禁止超配
- `0` 表示不允许超配（100% 限制）
- `>0` 表示允许超配的百分比

**调度时的影响**：
- 实际最大可用 = 节点容量 × (1 + 超配比例 / 100)
- 例如：16GB 内存 + 50% 超配 = 最大 24GB 可分配

### 4.6 健康状态告警链路：触发、抑制与恢复

#### 4.6.1 告警级别定义

**异常基类级别**：`app/Exceptions/DisplayException.php:17-20`

```php
public const LEVEL_DEBUG = 'debug';
public const LEVEL_INFO = 'info';
public const LEVEL_WARNING = 'warning';
public const LEVEL_ERROR = 'error';
```

**前端告警级别**：`config/prologue/alerts.php:19-24`

```php
'levels' => [
    'info',      // 信息提示
    'warning',   // 警告
    'danger',    // 危险/错误
    'success',   // 成功
],
```

#### 4.6.2 守护进程连接异常分级

**DaemonConnectionException 分级逻辑**：`app/Exceptions/Http/Connection/DaemonConnectionException.php:28-65`

```php
public function __construct(GuzzleException $previous, bool $useStatusCode = true)
{
    // 根据状态码决定日志级别
    $level = $this->statusCode >= 500 && $this->statusCode !== 504
        ? DisplayException::LEVEL_ERROR    // 5XX（非504）：ERROR 级别
        : DisplayException::LEVEL_WARNING; // 其他：WARNING 级别

    parent::__construct($message, $previous, $level);
}
```

**异常触发矩阵**：
| 状态码 | 场景 | 日志级别 | 用户提示 |
|--------|------|----------|----------|
| 无响应 | 连接超时/拒绝 | WARNING | "无法建立连接，请重试" |
| 4XX | 认证失败/参数错误 | WARNING | 提取 Wings 返回的具体错误 |
| 5XX（非504） | Wings 内部错误 | ERROR | "远程主机发生错误" + request_id |
| 504 | 网关超时 | WARNING | "无法建立连接，请重试" |
| 2XX 异常 | 响应格式错误 | ERROR | "远程主机通信错误" |

#### 4.6.3 告警抑制机制

**1. 会话闪存抑制**

所有管理后台操作的告警通过 Session Flash 机制传递：
- 存储键：`alert_messages`（`config/prologue/alerts.php:36`）
- 仅在当前请求和下一次请求间有效
- 相同类型的告警会被覆盖而非累积

**示例**：`app/Http/Controllers/Admin/NodesController.php:58`
```php
$this->alert->warning(trans('admin/node.notices.location_required'))->flash();
```

**2. 异常报告抑制**

**DisplayException::report()**：`app/Exceptions/DisplayException.php:67-80`

```php
public function report()
{
    // 仅当存在前置异常且该异常需要报告时才记录日志
    if (!$this->getPrevious() instanceof \Exception || !Handler::isReportable($this->getPrevious())) {
        return null;  // 抑制报告
    }
    // ... 记录日志
}
```

**3. 任务失败抑制**

**RunTaskJob::handle()**：`app/Jobs/Schedule/RunTaskJob.php:74-79`

```php
} catch (\Exception $exception) {
    // 仅当任务未设置 continue_on_failure 或异常非连接异常时才抛出
    if (!($this->task->continue_on_failure && $exception instanceof DaemonConnectionException)) {
        throw $exception;
    }
    // 否则静默继续下一个任务
}
```

**4. 调度状态抑制**

服务器处于以下状态时，所有操作被拦截并抛出 `ServerStateConflictException`：
- `STATUS_INSTALLING` - 安装中
- `STATUS_INSTALL_FAILED` - 安装失败
- `STATUS_SUSPENDED` - 已挂起
- `STATUS_RESTORING_BACKUP` - 恢复备份中
- `transfer != null` - 迁移中
- `node.maintenance_mode = true` - 节点维护中

**抑制效果**：不执行操作，直接返回状态冲突错误

#### 4.6.4 告警通知通道

**日志通道配置**：`config/logging.php:52-127`

| 通道 | 触发级别 | 用途 |
|------|----------|------|
| `daily` | debug+ | 每日轮转日志文件，保留 7 天 |
| `slack` | critical+ | Slack Webhook 告警（需配置） |
| `papertrail` | debug+ | Papertrail 远程日志 |
| `stderr` | debug+ | 标准错误输出 |
| `syslog` | debug+ | 系统日志 |

**邮件通知示例**：`app/Notifications/ServerInstalled.php:31-41`

```php
public function handle(Event|Installed $event): void
{
    $event->server->loadMissing('user');
    $this->server = $event->server;
    $this->user = $event->server->user;

    // 通过队列异步发送邮件
    Container::getInstance()->make(Dispatcher::class)->sendNow($this->user, $this);
}
```

#### 4.6.5 健康状态恢复条件

**1. 守护进程重启自动恢复**

**ServerDetailsController::resetState()**：`app/Http/Controllers/Api/Remote/Servers/ServerDetailsController.php:89-134`

```php
public function resetState(Request $request): JsonResponse
{
    // Wings 启动时自动调用，清理异常状态
    Server::query()->where('node_id', $node->id)
        ->whereIn('status', [Server::STATUS_INSTALLING, Server::STATUS_RESTORING_BACKUP])
        ->update(['status' => null]);  // 恢复为正常状态
}
```

**触发时机**：Wings 守护进程启动时自动调用

**2. 挂起状态恢复**

**SuspensionService::toggle()**：`app/Services/Servers/SuspensionService.php:28-60`

```php
// 解除挂起：数据库状态 → 同步 Wings → 失败回滚
$server->update(['status' => null]);
try {
    $this->daemonServerRepository->setServer($server)->sync();
} catch (\Exception $exception) {
    $server->update(['status' => Server::STATUS_SUSPENDED]);  // 回滚
    throw $exception;
}
```

**恢复条件**：Wings `sync` 接口调用成功

**3. 维护模式恢复**

通过节点设置页面关闭 `maintenance_mode` 即可恢复：
- 用户访问恢复正常
- 调度器自动恢复对该节点的考虑（需同时 `public = 1`）

**4. 任务失败自动恢复**

队列任务失败后满足以下条件可自动重试：
- 尝试次数 < `tries`（通常为 3）
- 异常类型为 `DaemonConnectionException`（连接问题）
- 未超过 `retry_after`（90 秒）窗口

#### 4.6.6 完整告警链路图

```
健康状态异常
    │
    ├─→ 异常抛出
    │    ├─→ DaemonConnectionException
    │    │    ├─→ 状态码判定
    │    │    │    ├─→ 5XX（非504）→ ERROR 级别日志
    │    │    │    └─→ 其他 → WARNING 级别日志
    │    │    └─→ X-Request-Id 追踪
    │    └─→ ServerStateConflictException
    │         └─→ 状态抑制，不执行操作
    │
    ├─→ 告警抑制检查
    │    ├─→ Session Flash（一次性）
    │    ├─→ 异常报告开关（reportable）
    │    ├─→ 任务 continue_on_failure 标记
    │    └─→ 服务器状态拦截
    │
    ├─→ 告警通知
    │    ├─→ 日志通道（daily / slack / syslog 等）
    │    ├─→ 管理后台 Alert 提示（danger/warning）
    │    └─→ 邮件通知（服务器安装完成等事件）
    │
    └─→ 恢复机制
         ├─→ Wings 重启 → resetState() 清理异常状态
         ├─→ 解除挂起 → sync() 同步 Wings
         ├─→ 关闭维护模式 → 用户访问恢复
         └─→ 队列重试 → 线性退避（10s → 20s → 30s）
```

## 5. 断连与恢复的状态机

### 5.1 守护进程认证机制

**DaemonAuthenticate 中间件**：`app/Http/Middleware/Api/Daemon/DaemonAuthenticate.php:34-66`

认证流程：
1. 从请求头提取 Bearer Token
2. Token 格式：`{daemon_token_id}.{daemon_token}`
3. 通过 `daemon_token_id` 查找对应节点
4. 解密并验证 `daemon_token` 是否匹配
5. 将节点对象注入请求属性

### 5.2 连接异常处理

**DaemonConnectionException**：`app/Exceptions/Http/Connection/DaemonConnectionException.php:28-65`

异常分级处理：
- **无响应**：返回 504 网关超时，提示"无法建立连接"
- **4XX 错误**：提取守护进程返回的具体错误信息
- **5XX 错误**：记录错误日志，包含 `X-Request-Id` 便于追踪
- **异常响应**：对 2XX 但实际错误的情况降级为 502 错误网关

**日志级别**：
- 5XX（非 504）：`ERROR` 级别
- 其他：`WARNING` 级别

### 5.3 守护进程重启状态恢复

**ServerDetailsController::resetState()**：`app/Http/Controllers/Api/Remote/Servers/ServerDetailsController.php:89-134`

```php
public function resetState(Request $request): JsonResponse
{
    // 1. 查找处于恢复备份状态的服务器
    $servers = Server::query()
        ->where('node_id', $node->id)
        ->where('status', Server::STATUS_RESTORING_BACKUP)
        ->get();

    // 2. 对异常中断的备份恢复记录失败日志
    foreach ($servers as $server) {
        Activity::event('server:backup.restore-failed')
            ->subject($server, $subject->subject)
            ->log();
    }

    // 3. 重置所有异常状态
    Server::query()->where('node_id', $node->id)
        ->whereIn('status', [Server::STATUS_INSTALLING, Server::STATUS_RESTORING_BACKUP])
        ->update(['status' => null]);
}
```

**触发时机**：Wings 守护进程启动时自动调用，用于清理因进程崩溃导致的异常状态。

### 5.4 服务器挂起与恢复

**SuspensionService::toggle()**：`app/Services/Servers/SuspensionService.php:28-60`

```php
public function toggle(Server $server, string $action = self::ACTION_SUSPEND): void
{
    // 1. 更新数据库状态
    $server->update([
        'status' => $isSuspending ? Server::STATUS_SUSPENDED : null,
    ]);

    try {
        // 2. 通知守护进程同步状态
        $this->daemonServerRepository->setServer($server)->sync();
    } catch (\Exception $exception) {
        // 3. 失败回滚
        $server->update([
            'status' => $isSuspending ? null : Server::STATUS_SUSPENDED,
        ]);
        throw $exception;
    }
}
```

**关键特性**：
- 采用**数据库优先**模式，先更新面板状态
- 调用守护进程 `/api/servers/{uuid}/sync` 接口同步
- **失败自动回滚**机制，保证数据一致性
- 迁移中的服务器禁止修改挂起状态

### 5.5 完整状态流转图

```
正常运行 (status=null)
    │
    ├─→ 安装中 (installing) ──┐
    │                         ├─→ 安装失败 (install_failed)
    │                         └─→ [完成] → 正常运行
    │
    ├─→ 恢复备份 (restoring_backup) ──┐
    │                                  ├─→ [失败] → 活动日志记录
    │                                  └─→ [完成/中断] → 正常运行
    │
    ├─→ 已挂起 (suspended) ←──────────┐
    │                                  │
    └─→ 迁移中 (transfer != null)      │
                                      │
[Wings 重启触发 resetState] ───────────┘
    │
    ├─→ 清除 installing / restoring_backup 状态
    └─→ 记录失败的备份恢复操作
```

## 6. 关键源码索引

| 模块 | 文件路径 | 核心类/方法 |
|------|----------|------------|
| 节点模型 | `app/Models/Node.php` | `isViable()`, `isUnderMaintenance()`, `getConnectionAddress()` |
| 服务器模型 | `app/Models/Server.php` | `validateCurrentState()`, `isSuspended()`, `STATUS_*` 常量 |
| 节点仓库 | `app/Repositories/Eloquent/NodeRepository.php` | `getUsageStats()`, `getNodeWithResourceUsage()` |
| 部署服务 | `app/Services/Deployment/FindViableNodesService.php` | `handle()` |
| 挂起服务 | `app/Services/Servers/SuspensionService.php` | `toggle()` |
| 资源查询 | `app/Http/Controllers/Api/Client/Servers/ResourceUtilizationController.php` | `__invoke()` |
| 守护进程回调 | `app/Http/Controllers/Api/Remote/Servers/ServerDetailsController.php` | `resetState()`, `list()`, `__invoke()` |
| 守护进程认证 | `app/Http/Middleware/Api/Daemon/DaemonAuthenticate.php` | `handle()` |
| 维护中间件 | `app/Http/Middleware/MaintenanceMiddleware.php` | `handle()` |
| 指标转换 | `app/Transformers/Api/Client/StatsTransformer.php` | `transform()` |
| 连接异常 | `app/Exceptions/Http/Connection/DaemonConnectionException.php` | 完整异常类 |
| 守护进程通信 | `app/Repositories/Wings/DaemonRepository.php` | `getHttpClient()` |
| 定时任务 | `app/Console/Kernel.php` | `schedule()` |
| 配置参数 | `config/pterodactyl.php` | `guzzle.timeout`, `guzzle.connect_timeout` |
| **前端心跳** | `resources/views/admin/nodes/index.blade.php` | `pingNodes()` JavaScript 函数 |
| **队列配置** | `config/queue.php` | `retry_after`, 队列连接配置 |
| **队列重试** | `app/Jobs/RevokeSftpAccessJob.php` | `tries`, `release()` 退避策略 |
| **告警基类** | `app/Exceptions/DisplayException.php` | `LEVEL_*` 常量, `report()` 抑制逻辑 |
| **告警配置** | `config/prologue/alerts.php` | `levels`, `session_key` |
| **日志通道** | `config/logging.php` | `daily`, `slack`, `syslog` 等通道配置 |
| **调度任务** | `app/Jobs/Schedule/RunTaskJob.php` | `continue_on_failure` 抑制逻辑 |
| **邮件通知** | `app/Notifications/ServerInstalled.php` | `handle()`, `toMail()` |

## 7. 设计要点总结

### 7.1 可靠性设计
- **缓存降级**：20秒缓存避免雪崩
- **失败回滚**：挂起操作的数据库状态回滚
- **优雅恢复**：Wings 重启时自动清理异常状态
- **请求追踪**：`X-Request-Id` 全链路追踪
- **多级超时**：前端 5s / 连接 5s / 请求 15s 三级超时保护
- **线性退避**：队列重试采用 10s → 20s → 30s 递增延迟

### 7.2 性能优化
- **数据库聚合**：使用 SQL `SUM()` 而非应用层计算
- **预加载**：服务器列表查询时预加载关联关系
- **批量更新**：状态重置使用批量 SQL 更新
- **前端轮询**：10秒轮询间隔平衡实时性与性能

### 7.3 安全机制
- **双向认证**：面板→Wings 使用节点 Token，Wings→面板使用相同机制
- **Token 加密**：`daemon_token` 在数据库中加密存储
- **权限检查**：远程 API 严格验证节点归属关系

### 7.4 告警设计
- **分级告警**：ERROR / WARNING 二级日志级别
- **多重抑制**：会话闪存、异常报告开关、任务 continue_on_failure、状态拦截四层抑制
- **多通道通知**：日志文件、Slack、Syslog、邮件多通道支持
- **自动恢复**：Wings 重启自动清理、队列重试、状态回滚三重恢复机制

### 7.5 心跳设计
- **双轨心跳**：管理后台前端轮询（10s）+ 后端按需调用
- **状态可视化**：绿色心跳动画 / 红色空心心直观展示
- **重试策略**：3次重试 + 线性退避，避免瞬时故障误判
- **任务去重**：`ShouldBeUnique` 接口防止重复任务入队
